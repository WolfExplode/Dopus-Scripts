~~Title: String~~
<evalcmd>
String && string && String of characters. &&
count && int && Number of characters. &&
char && string && Character to copy.
</evalcmd>

Returns a string consisting of //count// copies of the character //char//.

//Example://
<code>
Output(String(5, "A"));
--> AAAAA
</code>

//See also://
[[space]]
