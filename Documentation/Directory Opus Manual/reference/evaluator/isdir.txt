~~Title: IsDir~~
<evalcmd>
IsDir && bool && **True** if the specified path exists as a directory, otherwise **false**. &&
path && path && Path to test.
</evalcmd>

Tests if the specified path exists as a directory.

//Example://
<code>
if (IsDir("c:\\windows\\system32")) { ... }
</code>


