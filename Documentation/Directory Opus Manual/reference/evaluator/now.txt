~~Title: Now~~
<evalcmd>
Now && date && Current time and date.
</evalcmd>

Returns the current time and date.

//Example://
<code>
if (DatePart(Now(), "yyyy") == 2023) { ... } // is it still 2023?
</code>
